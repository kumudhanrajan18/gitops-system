
eks-chart:
  aws:
    region: us-west-2
  
  serviceAccount:
    create: true
    name: ack-eks-controller
    namespace: ack-system
    annotations: {}
  
  resources:
    limits:
      cpu: 500m
      memory: 256Mi
    requests:
      cpu: 250m
      memory: 128Mi
  
  tolerations:
    - key: "CriticalAddonsOnly"
      operator: "Exists"
  
  nodeSelector:
    kubernetes.io/os: linux

  webhook:
    enabled: true
    port: 9443
    
  deployment:
    namespace: ack-system
