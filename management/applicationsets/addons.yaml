apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: management-addons
  namespace: argocd
spec:
  generators:
  - list:
      elements:
      - name: ack-eks-controller
        namespace: ack-system
  template:
    metadata:
      name: addons
      namespace: argocd
    spec:
      project: default
      source:
        repoURL: https://github.com/kumudhanrajan18/gitops-system.git
        targetRevision: HEAD
        path: management/addons/ack
        helm:
          valueFiles:
          - values.yaml
          registrySecrets:
          - name: ecr-credentials
      destination:
        server: https://kubernetes.default.svc
        namespace: ack-system
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
